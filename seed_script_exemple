job("$name") {
  description ('an example Jenkins job created with Job dsl')
  displayName ("$name")
  scm {
    git{
         remote{
                 url ('https://github.com/yossiazu/t1.git')
         }
      }
    }
  triggers {
    scm('* * * * *')
  }
  steps {
    shell('sh 'ls -la' ')
  }
}
